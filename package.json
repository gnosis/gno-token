{
  "directories": {
    "lib": "contracts",
    "test": "test"
  },
  "scripts": {
    "prepack": "npm run contracts",
    "test": "truffle test",
    "truffle": "truffle",
    "networks-extract": "node scripts/extract_network_info.js",
    "networks-inject": "node scripts/inject_network_info.js",
    "networks-reset": "truffle networks --clean && node scripts/inject_network_info.js",
    "networks": "truffle networks",
    "compile": "eslint . && standard --fix && truffle compile",
    "deploy": "truffle deploy",
    "contracts": "npm run compile && npm run networks-reset",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-plugin-import": "^2.11.0",
    "eslint-config-standard": "^11.0.0",
    "eslint-plugin-node": "^6.0.0",
    "eslint-plugin-promise": "^3.7.0",
    "eslint-plugin-standard": "^3.1.0",
    "lodash": "^4.17.5",
    "truffle": "^4.1.5",
    "truffle-hdwallet-provider": "^0.0.5"
  }
}
